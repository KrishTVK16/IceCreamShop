PORT=3000

# SQL Server connection
# Use your computer name (from SQL Server Management Studio properties)
DB_SERVER=TVK-5559-PC
DB_NAME=RRzDBdev_1
DB_INSTANCE=SQLEXPRESS
# DB_PORT=1433  # Only needed if not using instance name

# Windows Authentication (RECOMMENDED - uses your Windows credentials)
# Leave DB_USER and DB_PASS empty for Windows Auth
DB_USER=
DB_PASS=

# SQL Server Authentication (Alternative - if Windows Auth doesn't work)
# Create a SQL login using create_sql_login.sql script, then set:
# DB_USER=rrz_app_user
# DB_PASS=YourPasswordHere

# Optional pool overrides
# DB_POOL_MAX=10
# DB_POOL_MIN=0
# DB_POOL_IDLE=30000

# Optional security flags
# DB_ENCRYPT=false
# DB_TRUST_CERT=true

# Allow comma separated origins, defaults to *
# CORS_ORIGIN=http://localhost:5500
